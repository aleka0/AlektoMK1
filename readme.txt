To run use command:
python3 main.py

if this is the first bot on the network enter 0, otherwise enter IP of exsisting bot.
This will update the bot to all IPs on the botnet then start idle listening mode.

To enter command and controll mode hit controll+c.

Once in controll mode type exit to disconect the bot or type help fpr more information.
